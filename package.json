{
  "name": "com.defi.defiwebapp",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter client dev",
    "build": "pnpm --filter client build"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}